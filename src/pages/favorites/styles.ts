import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  holder: {
    flex: 1,
  },
  emptyHolder: {
    padding: 20,
  },
  emptyHolderHeading: {
    fontSize: 18,
    paddingBottom: 10,
  },
});
